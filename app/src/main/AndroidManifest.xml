<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Ağ erişimi -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Android 13+ bildirim izni (33 ve üzeri için şart) -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MustiShop"
        tools:targetApi="34">

        <!-- Ana kabuk (fragment'ları barındırır) -->
        <activity
            android:name=".presentation.shell.MainActivity"
            android:exported="true" />

        <!-- Giriş/AUTH akışı -->
        <activity
            android:name=".presentation.feature_auth.AuthActivity"
            android:exported="true"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase Cloud Messaging alıcısı -->
        <service
            android:name=".service.MyFirebaseMessageService"
            android:exported="false">
            <intent-filter>
                <!-- Eski INSTANCE_ID_EVENT kaldırıldı; MESSAGING_EVENT kullanılmalı -->
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
