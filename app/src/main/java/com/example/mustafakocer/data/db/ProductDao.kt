package com.example.mustafakocer.data.db

import androidx.room.Dao
import androidx.room.Delete
import androidx.room.Insert
import androidx.room.Query
import com.example.mustafakocer.data.model.Product

@Dao
interface ProductDao {

/*
    @Insert
    fun insertProduct(selectedProduct: SelectedProduct): Long // sqlite return id

    @Delete
    fun deleteProduct(selectedProduct: SelectedProduct): Long // sqlite return id

    @Query("SELECT * FROM products")
    fun getAllProducts(): List<Product>
 */

}